# Private Output Model

V1 is private-output only. Runtime evidence is not committed by default and GitHub Actions uploads private workflow artifacts.

This keeps source control focused on the tool while allowing evidence capture to happen in a controlled workflow run.

Future versions may add explicit redacted or public publication modes, but they are intentionally not included in v1.

