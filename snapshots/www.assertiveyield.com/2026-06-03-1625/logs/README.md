# Logs

Structured logs from each tool step, plus GitHub Actions workflow logs when available. Logs use JSON Lines where possible so they can be searched, parsed, and compared. Start with `tool-log-summary.md` for an index of local tool logs.

Forensic value: logs show the operational sequence, timestamps, selected URLs, retries, warnings, and tool behavior during collection.
