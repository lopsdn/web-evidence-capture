# GitHub Actions Logs

Workflow metadata, job metadata, and text logs captured from the GitHub Actions run when available. GitHub may delete native Actions logs later, so this folder preserves a copy inside the snapshot.

Forensic value: these logs connect the committed snapshot back to the runner execution that produced it. The current job's final lines may be limited by what GitHub exposes while the job is still running.
