# Executive Summary

Target: `{{ config.target_url }}`

Case: `{{ config.case_slug }}`

Generated: {{ generated_local }} ({{ generated_utc }})

This package preserves public website evidence only. It records URL inventory, public HTTP capture, mirror output, WARC/WACZ packaging, screenshots, PDFs, privacy summaries, manifests, hashes, validation evidence, and logs where available.

Validation status: `{{ validation.status | default("not_run") }}`

Counts:

- Inventory URLs: `{{ summary.inventory_url_count }}`
- Captured pages: `{{ summary.captured_pages }}`
- Rendered pages: `{{ summary.rendered_pages }}`
- Render retries: `{{ summary.render_retries }}`
- WACZ pages detected: `{{ summary.wacz_pages_detected }}`

No authentication was attempted, no accounts were created, and no forms were submitted.

